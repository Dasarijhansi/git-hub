package com.nt;

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

public class ClassB {

	public static void main(String[] args) {
	
		List<String> list=Arrays.asList("2134","jdfg43","732","he72");
		list.stream().filter(data-> Character.isDigit(data)).collect(Collectors.toList()).forEach(System.out::println);
	    
	}
}