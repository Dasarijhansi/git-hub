package com.nt;

import java.util.Arrays;
import java.util.Comparator;
import java.util.List;
import java.util.stream.Collectors;

public class ClassB {

	public static void main(String[] args) {
	
		List<String> list=Arrays.asList("21","1433","732","172","13","167");
		list.stream().filter(da->da.startsWith("1")).map(Integer::parseInt).distinct().sorted(Comparator.reverseOrder()).collect(Collectors.toList()).forEach(System.out::println);;
	    
	}
}